`analyze_domain` orchestrates the rest: normalize domain → get_or_create Domain → cache lookup → concurrency guard → create PENDING Analysis → parse warm-up CSV → `_collect_all` → persist domain static fields + DomainMetric → `quick_score` → map verdict → `complete()` the analysis → write per-source ApiLogs → attach warm-up file. Wrapped in try/except that fails the analysis and re-raises as `PredictionError`.
