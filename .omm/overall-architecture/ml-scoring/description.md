The scoring subsystem that turns collected signals into a verdict. `services/scoring_service.py` coordinates: it runs the XGBoost risk model, optionally the warm-up profitability model, combines them in the `DecisionMatrix`, and attaches a SHAP explanation. Feature engineering (`ml/feature_builder.py`) keeps training/inference in lockstep. Detailed stage topology is in the `pipeline` perspective.
