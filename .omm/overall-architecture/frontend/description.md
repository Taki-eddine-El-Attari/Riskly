React 19 + Vite + TypeScript single-page app (`frontend/src/`). Strictly layered (documented in `frontend/ARCHITECTURE.md`): types mirror the backend Pydantic schemas exactly, an api/ layer does HTTP access, hooks wrap TanStack Query for cache/mutations, context holds auth state, and components/pages render prepared data. No JWT is kept in JS — the session rides an HttpOnly cookie.
