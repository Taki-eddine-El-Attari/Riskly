The scoring pipeline that converts raw signals into a verdict. Two independent XGBoost models run over a shared feature-engineering layer: a risk classifier (is the domain malicious?) and a warm-up classifier (is its email deliverability healthy?). Their outputs, plus authority signals, are fused by a rule-based `DecisionMatrix` into one of five internal verdicts, and a SHAP pass explains the risk contribution.
